name: Deploy to Render
on:
  push:
    branches:
      - main
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render Deploy
        run: curl -X POST "https://api.render.com/deploy/srv-d3hsdrqli9vc7397bur0?key=ASGAqKwdyys"
